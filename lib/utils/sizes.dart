import 'package:get/get.dart';

class Sizes {
  static double screenHeight = Get.context!.height;
  static double screenWidth = Get.context!.width;

  static double largeHeight = screenHeight / 2;
  static double largeWidth = screenHeight / 2;

  static double mediumHeight = screenHeight / 10;
  static double meiumWidth = screenHeight / 10;

  static double largeFontSize = screenHeight / 32;
  static double smallFontSize = screenHeight / 50;
}
